import SwiftUI
import Moya
import SDS

struct ContentView: View {
    var body: some View {
        VStack {
            Text("w")
                .font(.pretendard(.black, size: 50))
                .foregroundStyle(Color.primary(.light))
        }
    }
}

#Preview {
    ContentView()
}
